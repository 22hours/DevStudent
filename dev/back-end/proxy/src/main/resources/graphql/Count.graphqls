type Count{
    count : String
}

extend type Query {
    countAllQuestions : Count
    countUnreadAlarms(nickname : String) : Count
}

extend type Mutation {
    checkDuplicateNickname(nickname : String) : Count
    checkEmail(email : String) : Count
}